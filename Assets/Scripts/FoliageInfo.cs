using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoliageInfo : MonoBehaviour
{
    // Start is called before the first frame update
    public int fieldRow = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
